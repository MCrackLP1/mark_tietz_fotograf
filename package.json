{
  "name": "mark-tietz-fotograf-webseite",
  "version": "1.0.0",
  "description": "Professionelle Fotografenwebseite für Mark Tietz",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "postcss style.css -o dist/style.min.css --map",
    "build": "npm run build:css && npm run copy:files",
    "copy:files": "node -e \"const fs=require('fs'); const path=require('path'); if(!fs.existsSync('public')){fs.mkdirSync('public',{recursive:true})}; const copyDir=(src,dest)=>{if(!fs.existsSync(dest)){fs.mkdirSync(dest,{recursive:true})}; fs.readdirSync(src).forEach(item=>{const srcPath=path.join(src,item); const destPath=path.join(dest,item); if(fs.statSync(srcPath).isDirectory()){copyDir(srcPath,destPath)}else{fs.copyFileSync(srcPath,destPath)}})}; ['*.html','assets','css','js','dist','robots.txt','sitemap.xml','.htaccess'].forEach(pattern=>{if(pattern.includes('*')){fs.readdirSync('.').filter(f=>f.endsWith('.html')).forEach(f=>fs.copyFileSync(f,path.join('public',f)))}else if(fs.existsSync(pattern)){if(fs.statSync(pattern).isDirectory()){copyDir(pattern,path.join('public',pattern))}else{fs.copyFileSync(pattern,path.join('public',pattern))}}})\"",
    "dev": "python -m http.server 8000",
    "optimize:html": "node -e \"console.log('HTML optimization placeholder - implement minification if needed')\""
  },
  "keywords": ["fotografie", "photography", "allgau", "kempten", "hochzeit", "business"],
  "author": "Mark Tietz",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.17"
  }
}
